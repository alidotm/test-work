import { FC } from "react";

const Loading: FC = () => {
	return (
		<div className="fixed bg-white z-[9999] top-0 left-0 right-0 bottom-0 w-screen h-screen">
			Loading...
		</div>
	);
};

export default Loading;
